# Pixel card dashboard Builder

Duty: Owns implementation inside the validated ShipSpec contract.

## Notes

- Implementation is already present in the active diff and remains scoped to the local demo app.
- `src/quest-board.js` tracks defaulted priority, total XP, current streak, base XP, and streak bonus XP.
- `src/app.js` renders priority labels, streak state, rank/progress, completion messages, and keeps direct file-open behavior aligned with the module implementation.
- `src/styles.css` provides the pixel-card visual treatment and responsive quest-card layout.
